export default class Ball extends Phaser.Group {
    constructor(game) {
        super(game);
        this.game = game;
        this.init();
    }
    init() {

        // this.ball.body.velocity.set(this.speed, -this.speed);


    }
}